/**
 * Convx Project (C) 2026
 * Licensed under GPL-3.0 | See git history for contributors
 */

package com.convx.music.constants

/** How the library screen's background renders when no custom home-background
 *  image is set (see HomeBackgroundControls / LibraryBackgroundModeKey). */
enum class LibraryBackgroundMode {
    PLAIN,
    THEME,
    THUMBNAIL_BLUR,
}
