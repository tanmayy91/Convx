<?xml version="1.0" encoding="utf-8"?>
<!--
  updatePeriodMillis=0: pushed from MusicService on playback events, never polled.
  The system minimum is 30 min anyway, which would be useless for a now-playing
  card and would only wake the app for nothing.

  minWidth/minHeight allow the smallest form (the 4x1 row); targetCellWidth/Height
  (API 31+) ask the launcher to place it at 4x2 initially. The layout for any given
  size comes from the mapped RemoteViews built in the widget manager, so resizing
  is handled inside the launcher without waking the app.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="40dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:maxResizeWidth="400dp"
    android:maxResizeHeight="220dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="0"
    android:configure="com.convx.music.widget.WidgetConfigActivity"
    android:widgetFeatures="reconfigurable|configuration_optional"
    android:previewLayout="@layout/widget_now_playing_wide"
    android:initialLayout="@layout/widget_now_playing_wide"
    android:description="@string/widget_now_playing_description"
    android:widgetCategory="home_screen"
    android:resizeMode="horizontal|vertical" />
