# Feature switches for the Android app.
#
# Set allSettings=true when the user customization settings should be visible.
# This file is read during the Gradle build, so changing it requires a rebuild.
allSettings=true